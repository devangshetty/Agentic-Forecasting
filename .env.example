# Copy to .env and fill values (do NOT commit .env)
# Path inside container (we will mount the project root as /app)
DATA_PATH=/app/stores_sales_forecasting.pandas.csv

# OpenAI key (optional; keep secret)
# OPENAI_API_KEY=sk-...

# Service host/port (python service)
ARIMA_HOST=forecast_service
ARIMA_PORT=8000

# Ruby model params (optional)
LAGS=14
MODEL_N_ESTIMATORS=200
MODEL_MAX_DEPTH=8
TRAIN_FRAC=0.8
